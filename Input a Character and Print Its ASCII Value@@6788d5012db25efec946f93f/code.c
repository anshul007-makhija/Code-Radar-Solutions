#include <stdio.h>

int main() {
    char character;

    // Input a single character
    scanf("%c", &character);

    // Output the ASCII value of the character
    printf("ASCII Value : %d\n", character, character);

    return 0;
}
