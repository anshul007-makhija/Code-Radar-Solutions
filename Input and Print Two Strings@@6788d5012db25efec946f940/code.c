#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    string x=[5];
    string y=[5];
    scanf("%s",&x);
    scanf("%s",&y);
    printf("%s",x);
    printf("%s",y);
    return 0;
}