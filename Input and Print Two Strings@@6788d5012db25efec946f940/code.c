#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    string x=[];
    string y=[];
    scanf("%s",&x);
    scanf("%s",&y);
    printf("%s",x);
    printf("%s",y);
    return 0;
}